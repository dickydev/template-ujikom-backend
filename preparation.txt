1. FRONTEND
2. DATABASE DAN REST API
3. BACKEND

TEMA : 
-.
-.
-.

SIKLUS :> LANDING PAGE & DASHBOARD (FRONTEND) -> DATABASE (SQL) -> BACKEND (REST API) -> CONSUME KE FRONTEND

SISTEM : 
1. SISTEM LOGIN
2. SISTEM PENDATAAN
3. CRUD (CREATE READ UPDATE DELETE)
4. LANDING PAGE (READ SEMUA DATA)
5. DASHBOARD (MENGELOLA SEMUA DATA)

CONTOH 
DATA : BUKU, ANGGOTA, TEMPAT, DLL


BACKEND => FRAMEWORK (EXPRESS JS) => LIBRARY : express mysql2 argon2/bycrypt body-parser cors jsonwebtoken cookie-parser ...

PATTERN => MVC (MODEL VIEW CONTROLLER) => MODELS, CONTROLLERS, ROUTES 